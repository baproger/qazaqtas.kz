<?php

namespace App\Services\Ai;

/**
 * Ошибка помощника, которую МОЖНО показать пользователю: текст уже написан
 * человеческим языком («ключ не настроен», «лимит исчерпан»). Всё остальное
 * летит обычными исключениями в лог.
 */
class AssistantException extends \RuntimeException {}
